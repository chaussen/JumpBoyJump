using UnityEngine;
using System.Collections;

public class MusicAdjust : MonoBehaviour {
/* HERE
	private GameObject _gameObject;
	private MusicManagement _music;
	private static float _volume = 1.0f;

	void Awake()
	{
		
		_gameObject = GameObject.Find ("_managerMainMenu");
		//_music = _gameObject.GetComponent ("MusicManagement");
		_music = _gameObject.GetComponent<MusicManagement> ();
		
		//_music.transform.GetComponent<MusicManagement>();
	}

	void Update ()
	{
		if (_music.GetType () == typeof(MusicManagement)) {
			_music.UpdateAdjusment (_volume);
		}
	}
	public static void AdjustVolume(float NewVolume)
	{
		_volume = NewVolume;
	} HERE*/
}
