using UnityEngine;
using System.Collections;

public class ChangeItem : MonoBehaviour {
/* HERE
	public Sprite[] _sprites;
	private GameObject _hatSelection;
	private Component _imageSource;
	int _clicks = 0;

	// Use this for initialization
	void Start () {
	
		_hatSelection = gameObject.Find("_hatSelection");
		//_hatSelection = this;
		_imageSource = _hatSelection.GetComponent<Image>();

		_sprites [0] = Resources.Load ("Red_Hat") as Texture2D;
		_sprites [1] = Resources.Load ("Purple_Hat") as Texture2D;

	}

	public void ChangeHat(int Increment)
	{
		_clicks += Increment;

		if (_clicks == 0) {
			_imageSource.sprite = _sprites[0];
		}
		if (_clicks == 1) {
			_imageSource.sprite = _sprites[1];
		}
		if (_clicks == 2) {
			_clicks = 0;
			_imageSource.sprite = _sprites[0];
		}
	}

	
	// Update is called once per frame
	void Update () {
	
	}HEREE*/
}
